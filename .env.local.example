# =========================================================================
# .env LOCAL — Frontend (cole como .env na raiz de frontend/)
# =========================================================================
# O Vite proxy (vite.config.ts) encaminha /api → http://localhost:3000
# Então NÃO precisa de URL absoluta aqui.

VITE_API_URL=/api
